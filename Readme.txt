Libraries used
import os
import numpy as np
import pickle
import sys
import warnings
from sklearn.externals import joblib

Note: Model was trained using kaggle and googe colab to have better speed, so the complete file is text-classifier
which can be open in jupyter notebook.
The alternative of this file is named as function.py 

